Test Unit NMCore:
  stage: test
  image: $CI_REGISTRY_IMAGE/ci-build:latest
  before_script:
  - cmake --preset Test
  script:
  - cmake --build --preset Test --target TestUnitNMCoreCobertura
  - cat ./Build/coverage/TestUnitNMCore/index.txt
  - echo "Total Coverage $(grep -oE 'TOTAL\s+[0-9]+\s+[0-9]+\s+([0-9.]+%)' ./Build/coverage/TestUnitNMCore/index.txt | awk '{ print $4 }')"
  coverage: /Total Coverage (\d+\.\d+%)/
  artifacts:
    when: always
    paths:
    - ./Build/coverage
    reports:
      junit: ./Build/Test/UnitCore/default.xml
      coverage_report:
        coverage_format: cobertura
        path: Build/coverage/**/cobertura.xml
  cache:
    paths:
    - ./Build/
    - ./Tools/vcpkg/packages
    policy: pull

Test Unit NMM:
  stage: test
  image: $CI_REGISTRY_IMAGE/ci-build:latest
  before_script:
  - cmake --preset Test
  script:
  - cmake --build --preset Test --target TestUnitNMMathCobertura
  - cat ./Build/coverage/TestUnitNMMath/index.txt
  - echo "Total Coverage $(grep -oE 'TOTAL\s+[0-9]+\s+[0-9]+\s+([0-9.]+%)' ./Build/coverage/TestUnitNMMath/index.txt | awk '{ print $4 }')"
  coverage: /Total Coverage (\d+\.\d+%)/
  artifacts:
    when: always
    paths:
    - ./Build/coverage
    reports:
      junit: ./Build/Test/UnitNMM/default.xml
      coverage_report:
        coverage_format: cobertura
        path: Build/coverage/**/cobertura.xml
  cache:
    paths:
    - ./Build/
    - ./Tools/vcpkg/packages
    policy: pull
