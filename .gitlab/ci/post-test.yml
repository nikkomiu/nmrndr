HTML Coverage:
  stage: post-test
  image: archlinux:latest
  before_script:
  - pacman -Sy --noconfirm lcov
  script:
  - ./Tools/scripts/merge_lcov.sh
  artifacts:
    when: always
    paths:
    - ./Build/coverage
  cache:
    paths:
    - ./Build/
    - ./Tools/vcpkg/packages
    policy: pull
